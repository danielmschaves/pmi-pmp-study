services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - .:/workspace          # live-edit notebooks and scripts from host
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    restart: unless-stopped
